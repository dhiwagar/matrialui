import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1760a5",
      light: "skyBlue",
    },
    secondary: {
      main: "#15c60",
    },
    otherColor: {
      main: "#999",
    },
    shape: {
      borderRadius: 8,
    },
  },
});
