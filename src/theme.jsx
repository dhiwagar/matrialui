import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#a51717",
      light: "skyBlue",
    },
    secondary: {
      main: "#cc1111660",
    },
    otherColor: {
      main: "#999",
    },
    shape: {
      borderRadius: 8,
    },
  },
});
